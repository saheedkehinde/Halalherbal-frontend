[build]
  command = "npm run build"
  publish = "client/dist"
  # Optional ignore command; must be a string
  ignore = "echo 'always build'"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
